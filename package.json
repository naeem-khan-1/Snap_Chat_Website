{
  "name": "snapsaver",
  "version": "1.0.0",
  "description": "SnapSaver — Public Snapchat story viewer and downloader",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "node dev-server.js",
    "vercel:dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "express": "^4.18.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "vercel": "^54.15.0"
  }
}
